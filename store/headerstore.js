export const state = () => ({
    signedin:false
  })
  
  export const getters = {
    getState(state) {
      return state.signedin
    }
  }
  
  export const mutations = {
    
  }
  
  export const actions = {
  
  }