export const state = () => ({
    signedin:true
  })
  
  export const getters = {
    getState(state) {
      return state.signedin
    }
  }
  
  export const mutations = {
    
  }
  
  export const actions = {
  
  }